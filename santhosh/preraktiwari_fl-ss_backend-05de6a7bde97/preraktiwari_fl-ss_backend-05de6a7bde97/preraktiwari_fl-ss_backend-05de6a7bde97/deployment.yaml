apiVersion: v1
kind: Service
metadata:
  name: surshri-service
spec:
  selector:
    app: surshri
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer  # Exposes the service externally via a cloud provider’s load balancer
